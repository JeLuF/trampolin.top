# trampolin.top

main.js connects to a baltic instance. Run via `node main.js`

Edit config.yaml to set db params

Single page web app is in public/index.html

Data gets synced to the web page by running mirror.sh
